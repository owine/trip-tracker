# Amtrak parser pack

Handles `@amtrak.com`. Last verified: 2026.

Train station names are free-text (no IATA-style code system for US rail).
Stored in `start_location.name`. Phase 4 may add a station→geo lookup.
